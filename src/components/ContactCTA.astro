---
interface Props {
  title: string;
  text: string;
  button?: string;
}

const {
  title,
  text,
  button = "Hablemos"
} = Astro.props;
---

<section class="contact-cta">
  <h2>{title}</h2>
  <p>{text}</p>
  <a class="cta" href="/contacto">{button}</a>
</section>
